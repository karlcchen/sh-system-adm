#!/bin/bash
#
#
LOG_BASE_DIR="uc-log"
LOG_BASE_FILE="uc"
#
mkdir -p ~/${LOG_BASE_DIR}/${LOG_BASE_FILE}0
mkdir -p ~/${LOG_BASE_DIR}/${LOG_BASE_FILE}1
mkdir -p ~/${LOG_BASE_DIR}/${LOG_BASE_FILE}2
mkdir -p ~/${LOG_BASE_DIR}/${LOG_BASE_FILE}3
mkdir -p ~/${LOG_BASE_DIR}/${LOG_BASE_FILE}4
mkdir -p ~/${LOG_BASE_DIR}/${LOG_BASE_FILE}5
mkdir -p ~/${LOG_BASE_DIR}/${LOG_BASE_FILE}6
mkdir -p ~/${LOG_BASE_DIR}/${LOG_BASE_FILE}7
mkdir -p ~/${LOG_BASE_DIR}/${LOG_BASE_FILE}8
mkdir -p ~/${LOG_BASE_DIR}/${LOG_BASE_FILE}9
